## Dev environment tips
- If you need to use node, you should use nvm.
- Avoid creating text files explaning your changes.
- Don't implemented nostr related code if it's already available in nostr-tools

## Nostr preferences
- Display npubs instead of hex keys